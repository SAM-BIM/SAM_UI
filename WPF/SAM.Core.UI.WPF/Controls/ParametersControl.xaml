<UserControl x:Class="SAM.Core.UI.WPF.ParametersControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:SAM.Core.UI.WPF"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="320">
    <DataGrid x:Name="DataGrid_Main"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              CanUserReorderColumns="False"
              HeadersVisibility="Column"
              GridLinesVisibility="All"
              SelectionMode="Single"
              SelectionUnit="Cell"
              RowHeaderWidth="0">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Parameter" Binding="{Binding Name, Mode=OneWay}" IsReadOnly="True" Width="*"/>
            <DataGridTemplateColumn Header="Value" Width="*">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding ValueText, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                 IsReadOnly="{Binding EffectiveReadOnly}"
                                 ToolTip="{Binding Description}"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 VerticalContentAlignment="Center"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>
        <DataGrid.GroupStyle>
            <GroupStyle>
                <GroupStyle.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="2" Background="#FFEFEFEF" Padding="3,1"/>
                    </DataTemplate>
                </GroupStyle.HeaderTemplate>
            </GroupStyle>
        </DataGrid.GroupStyle>
    </DataGrid>
</UserControl>
